# Create a student class that takes name and 3 subject marks as input and calculates the average marks.
class Student:
    def __init__(self,name,marks):
        self.name = name
        self.marks = marks
    def calculate_average(self):
        return sum(self.marks)/len(self.marks)
    
# Create an object of the student class and pass the name and marks as arguments.
s1 = Student("John", [80,70,90])

# Call the calculate_average method to get the average marks.
print("Average marks of", s1.name, "is", s1.calculate_average()) # Output: 80.0

# Create another object of the student class and pass the name and marks as arguments.
s2 = Student("Jane", [75,85,95])

# Call the calculate_average method to get the average marks.
print("Average marks of", s2.name, "is", s2.calculate_average()) # Output: 85.0
